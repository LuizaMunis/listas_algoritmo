#include <stdio.h>
int main(){
int A,B,C,D;
int diferenca;

printf("Digite quatro numeros inteiros:");
scanf("%d%d%d%d", &A,&B,&C,&D );

diferenca = (A*B)-(C*D), diferenca;

printf("%d*%d - %d*%d\n",A,B,C,D,diferenca);
printf("%d", diferenca);

}